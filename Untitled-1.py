def calc():
    str = int(input("*** 수식을 입력하세요 : "))
    for i in range(str.len):
        if(str[i].isdigit()):
            if(str[i+1].isdigit()):

        if(str[i] == '+'):

    

n = input("1.입력한 수식 계산 2.두 수 사이의 합계")
if(n == '1'):

#1+2/4

입력받아 str
J, i, k를 static으로 0 초기화
숫자, 기호를 순서대로 나눠
for i in range(str.len):
    if(str[i] == 숫자입니까?):
        while(str[i+1] == 숫자입니까?):
            N에다가 = 지금 인덱스를 숫자로 바꾸고 10을 곱해 그리고 다음 인덱스를 더해
            i =+ 1
        
        str2[j] = N
        j =+1
    elif(str[i] == 기호 네 가지 중 하나입니까?):
        
        str3[k] = str[i]
        k =+1

for i in range(str2.len):
    if(str3[i] == /, *기호가 있습니까?):
        if(str3[i] == "/"):
            result = str2[i] / str2[i+1]
        if(str3[i] == "*"):
        
0
01
1

1+2+3/4+5

2
2 3
0
0 1
1
1 2
3
3 4


for i in range(str.len):
    print(i, str[i])
    while (다음 입력(i+1)이 숫자입니까?)
       N에다가 = 지금 인덱스를 숫자로 바꾸고 10을 곱해 그리고 다음 인덱스를 더해
       i =+ 1

    if(다음 입력(i)이 기호이면)
        if(n이 +이면)
        while ((i+1) 입력이 숫자입니까?)
            Z에다가 =  인덱스를 숫자로 바꾸고 10을 곱해 그리고 다음 인덱스를 더해
        N = N + 
        현재 수에 기호 연산을 하는데 다음 수도 더해야하잖아. 젠장